services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./infra/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  backend:
    build: ./backend
    env_file: .env
    environment:
      # wewnątrz sieci dockera baza jest pod hostem "db", nie "localhost"
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    ports:
      - "8080:8080"
    depends_on:
      - db
    volumes:
      # cache pobranego modelu embeddingów, żeby nie ściągać przy każdym restarcie
      - ./models:/root/.cache/huggingface

  # ---------------------------------------------------------------------------
  # Serwowanie modelu Qwen3 przez vLLM.
  # Odkomentuj TYLKO jeśli masz GPU NVIDIA + nvidia-container-toolkit.
  # Bez GPU uruchom Ollama na hoście (ollama serve) i ustaw LLM_BASE_URL w .env.
  # ---------------------------------------------------------------------------
  # vllm:
  #   image: vllm/vllm-openai:latest
  #   command: >
  #     --model Qwen/Qwen3-1.7B
  #     --served-model-name qwen3
  #     --max-model-len 8192
  #     --gpu-memory-utilization 0.85
  #     --reasoning-parser qwen3
  #     --default-chat-template-kwargs {"enable_thinking": false}
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./models:/root/.cache/huggingface
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

volumes:
  pgdata:
